# 2026-02-06

## ClawDeck Self-Hosted Setup - COMPLETE ✅

### Installation Summary

**Location:** `/Users/northsea/clawdeck`
**Status:** Running successfully on http://127.0.0.1:3000

### Installed Components

1. **Ruby 3.3.10** (via Homebrew)
   - Path: `/opt/homebrew/opt/ruby@3.3/bin/ruby`
   - Status: ✅ Installed and working

2. **PostgreSQL 16** (via Homebrew)
   - Path: `/opt/homebrew/opt/postgresql@16/bin/psql`
   - Status: ✅ Running as service
   - Database: `clawdeck_development` and `clawdeck_test` created

3. **ClawDeck Application**
   - Version: Latest from GitHub (Rails 8.1.0)
   - Server: Puma 7.1.0
   - Status: ✅ Running on port 3000

### Setup Commands Executed

```bash
# 1. Installed dependencies
brew install ruby@3.3 postgresql@16

# 2. Set up PATH
export PATH="/opt/homebrew/opt/ruby@3.3/bin:/opt/homebrew/opt/postgresql@16/bin:$PATH"

# 3. Installed Bundler
gem install bundler

# 4. Started PostgreSQL
brew services start postgresql@16

# 5. Cloned ClawDeck repo
cd /Users/northsea
git clone https://github.com/clawdeckio/clawdeck.git

# 6. Installed gems
cd clawdeck
bundle install

# 7. Prepared database
bin/rails db:prepare

# 8. Started server
bundle exec rails server -p 3000
```

### Access Information

- **Local URL:** http://127.0.0.1:3000
- **Environment:** Development
- **Process ID:** 68380
- **Ruby Version:** 3.3.10
- **Rails Version:** 8.1.0
- **Web Server:** Puma 7.1.0

### Authentication

ClawDeck supports:
- ✅ Email/Password (works out of the box)
- GitHub OAuth (optional, requires setup)

### Next Steps

1. **Access the Application**
   - Open browser to: http://127.0.0.1:3000
   - Sign up with email/password
   - Create your first board

2. **Make Persistent** (Optional)
   - Add to shell startup (.zshrc):
     ```bash
     export PATH="/opt/homebrew/opt/ruby@3.3/bin:/opt/homebrew/opt/postgresql@16/bin:$PATH"
     ```
   - Start PostgreSQL on login:
     ```bash
     brew services start postgresql@16
     ```

3. **Auto-start Server** (Optional)
   - Create a launchd service for auto-startup on boot
   - Or use a process manager like foreman

4. **Production Deployment** (Future)
   - Set up environment variables
   - Configure GitHub OAuth (optional)
   - Deploy to web server or VPS
   - Use reverse proxy (nginx/Apache)

### File Locations

- **Application:** `/Users/northsea/clawdeck`
- **Database:** PostgreSQL data in `/opt/homebrew/var/postgresql@16`
- **Logs:** `/Users/northsea/clawdeck/log/development.log`
- **PID:** Process running in background session

### API Access

ClawDeck exposes a REST API for agent integrations:
- Get API token from Settings in the app
- Use `Authorization: Bearer YOUR_TOKEN` header
- Include agent identity headers:
  - `X-Agent-Name: YourAgentName`
  - `X-Agent-Emoji: 🦊`

### Process Management

To stop the server:
```bash
# Find the process
ps aux | grep "rails server"

# Or kill session if using tmux/process tool
```

To restart:
```bash
cd /Users/northsea/clawdeck
export PATH="/opt/homebrew/opt/ruby@3.3/bin:$PATH"
bundle exec rails server -p 3000
```

---

**Status:** ✅ Self-hosted ClawDeck is fully operational and ready for use!
